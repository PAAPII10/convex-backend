# PostgreSQL Connection String (without database name)
# Format: postgresql://username:password@host:port
# 
# For Supabase:
# - Connection Pooler (recommended, no IP allowlisting needed):
#   postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:6543
# - Direct Connection (requires IP allowlisting in Supabase dashboard):
#   postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432
#
# IMPORTANT: Remove the database name (/postgres) from the end if Convex creates its own DB
# Example: postgresql://postgres:password@db.rxzzrjokpozbmdzunkcr.supabase.co:6543
DATABASE_URL=postgresql://postgres:password@db.rxzzrjokpozbmdzunkcr.supabase.co:6543

# Admin Key for dashboard access and deployments
# Generate using: ./generate-admin-key.sh
CONVEX_SELF_HOSTED_ADMIN_KEY=

# Instance Secret for encryption/identity
# Generate a random 32+ character string
INSTANCE_SECRET=

# Convex API Origin (default: http://localhost:3210)
CONVEX_CLOUD_ORIGIN=http://localhost:3210

# Convex Site/Webhook Origin (default: http://localhost:3211)
CONVEX_SITE_ORIGIN=http://localhost:3211
